<%= simple_form_for User.new, remote: true, validate: true do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.button :submit, "Submit", id: 'create_user' %>
<% end %>
